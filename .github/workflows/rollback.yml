name: Rollback Workflow

on:
  workflow_dispatch:   # manual trigger only

jobs:
  rollback:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code (previous commit later)
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Checkout previous commit manually
      - name: Checkout previous commit
        run: git checkout HEAD~1

      # Step 3: Any rollback commands (example)
      - name: Deploy rollback
        run: echo "Rollback steps here"
